name: Run Tests

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install FFmpeg and ss
      run: |
        sudo apt update
        sudo apt install -y ffmpeg iproute2

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest httpx

    - name: Check if processes are using port 8000 using ss
      run: |
        sudo ss -tuln | grep ':8000' || echo "No process found on port 8000"

    - name: Kill processes using port 8000 if any
      run: |
        PIDS=$(sudo ss -tuln | grep ':8000' | awk '{print $6}')
        if [ ! -z "$PIDS" ]; then
          echo "Killing processes on port 8000"
          sudo kill -9 $PIDS
        else
          echo "No processes found on port 8000"
        fi

    - name: Start FastAPI server
      run: |
        screen -dm uvicorn 1.main:app --host 127.0.0.1 --port 8000
        sleep 10

    - name: Run tests
      run: |
        pytest --maxfail=1 --disable-warnings -v